// -------------------------------------------------------------------------
// Faulty - 'extend' used instead of 'extends' (invalid keyword)
// -------------------------------------------------------------------------

model Foo is {
    partial class Equipment is {
        String id;
    };

    class Pump is {
        Real c is external;
    } extend Equipment;
};
